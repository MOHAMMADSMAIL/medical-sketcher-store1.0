async function getProducts(){const r=await fetch(`${process.env.API_URL||process.env.NEXT_PUBLIC_API_URL||'http://localhost:3000'}/api/products`,{cache:'no-store'});if(!r.ok)throw new Error('Catalog unavailable');return r.json();}
export default async function Shop(){const products=await getProducts();return <main style={{padding:'3rem'}}><h1>Shop</h1><div style={{display:'grid',gridTemplateColumns:'repeat(3,1fr)',gap:'1rem'}}>{products.map((p:any)=><a key={p.id} href={`/shop/${p.slug}`}><article><h2>{p.title}</h2><p>{p.author?.name}</p><p>${Number(p.price).toFixed(2)}</p></article></a>)}</div></main>}
