# النسخ الاحتياطي والاسترجاع

لا يعتبر Docker volume نسخة احتياطية. اختر أحد الخيارين:

1. **Supabase Pro/PITR:** فعّل النسخ الاحتياطي وPITR من لوحة Supabase، وراجع مدة الاحتفاظ المتاحة في خطتك.
2. **pg_dump:** شغّل `scripts/backup-postgres.ps1` يومياً عبر Task Scheduler مع `DATABASE_URL`، ثم ارفع ملف `.dump` إلى مخزن خارجي مشفر (مثل S3 أو Backblaze). احتفظ يومياً 30 يوماً، وأسبوعياً 12 أسبوعاً، وشهرياً 12 شهراً.

## اختبار الاسترجاع (شهرياً)

1. أنشئ قاعدة PostgreSQL فارغة **غير إنتاجية**.
2. اجعل `DATABASE_URL` يشير إليها فقط.
3. شغّل: `./scripts/restore-postgres.ps1 -BackupFile ./backups/aurelia-YYYYMMDD-HHMMSS.dump`.
4. شغّل `npm run db:generate` ثم `npm run check` وتحقق من عدد المستخدمين والطلبات والكتب.
5. سجّل تاريخ الاختبار والنتيجة ومن نفذه.

## تخزين الملفات

Supabase Storage منفصل عن PostgreSQL. فعّل النسخ/التكرار لدى مزود التخزين أو نفّذ تصديراً دورياً لمفاتيح bucket إلى مخزن خارجي. اختبر استرجاع ملف PDF واحد شهرياً.

## تنبيه

لا تسترجع فوق قاعدة الإنتاج قبل تجربة النسخة على قاعدة مؤقتة والتحقق منها.
